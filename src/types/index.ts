/**
 * Core TypeScript type definitions
 * Re-exports types from shared module to ensure consistency across platforms
 */

// Re-export all shared types
export * from "@shared/types";

// Platform-specific type augmentations can go here if needed in the future
